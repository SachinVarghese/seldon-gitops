apiVersion: machinelearning.seldon.io/v1alpha2
kind: SeldonDeployment
metadata:
  creationTimestamp: null
  labels:
    app.kubernetes.io/instance: seldon-gitops-yaml-test
    fluentd: "true"
  name: iris
  namespace: yaml-test
spec:
  annotations:
    seldon.io/engine-seldon-log-messages-externally: "true"
  predictors:
  - componentSpecs:
    - metadata:
        creationTimestamp: "2020-10-14T08:54:27Z"
      spec:
        containers:
        - image: seldonio/sklearnserver_rest:1.3.0-dev
          name: iris-container
          ports:
          - containerPort: 6000
            name: metrics
            protocol: TCP
          resources: {}
          volumeMounts:
          - mountPath: /etc/podinfo
            name: seldon-podinfo
    engineResources: {}
    graph:
      endpoint:
        service_host: localhost
        service_port: 9000
        type: REST
      implementation: SKLEARN_SERVER
      logger:
        mode: all
      modelUri: gs://seldon-models/sklearn/iris
      name: iris-container
      type: MODEL
    labels:
      version: default
    name: default
    replicas: 1
    svcOrchSpec: {}
    traffic: 100
  protocol: seldon
  transport: rest
status: {}
